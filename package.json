{
  "private": true,
  "name": "boilerplate",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "comlink": "^4.3.0",
    "dexie": "^3.0.3",
    "preact": "^10.1.0",
    "preact-bulma": "^2.0.0-next.3",
    "preact-i18n": "^2.3.0-preactx",
    "preact-render-to-string": "^5.1.2",
    "unstated-next": "^1.1.0",
    "worker-loader": "^3.0.6"
  }
}
